# Name of the site, used in title (required)
title: Example site

# Full URL of the site with trailing slash (required)
url: http://example.com/

# Meta description (optional)
description: Description of an example site made with Stagger.

# Favicon (optional)
icon: favicon.png

# Site language (optional)
lang: en

# CSS-files
css: [
  spectre.css,
  spectre-icons.css,
  style.css
]

# JS-files
js: [
  app.js
]

# Menu
menu: [
  home,
  about,
  blog
]

# CSS-classes for given HTML elements
classes: {
  table: table
}
